#include "monty.h"
/**
 * f_stack - a function that prints the top of the stack head
 * @head: stack head
 * @counter: line_num
 * Return: no returns
*/

void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
